Async equivalent of `ncurses::getch` function
=============================================

This crate provides async version of `getch` function.

Refer to the [example](examples/ex1.rs) to see the usage.